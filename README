PEOPLEBOOK

Peoplebook is a simple address book application. The application currently
contains a single controller called people. This controller has an
associated model and view. The following fields are contained in the
people module: f_name, l_name, bio, box_number, address, phone_number, image_url.

Current plans include,
	Cleaning up the code and writing a stylesheet for the people view,
	Adding authentication using the Authlogic module.

Future plans include,
	Adding a basic messaging feature that incorporates email like features.
